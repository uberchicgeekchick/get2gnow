Index: AUTHORS
===================================================================
--- AUTHORS	(revision 373)
+++ AUTHORS	(working copy)
@@ -7,7 +7,10 @@
 
  * Brian Pepple  <bpepple@fedoraproject.org>
 
+--- Various fixes, patches, & improvements
 
+ * Kaity G. B. <uberChicGeekChick@openSUSE.us>
+
 --- Translators:
 
  * Swedish - Daniel Nylander <po@danielnylander.se>
Index: src/twitux-send-message-dialog.c
===================================================================
--- src/twitux-send-message-dialog.c	(revision 373)
+++ src/twitux-send-message-dialog.c	(working copy)
@@ -138,8 +138,7 @@
 	twitux_debug (DEBUG_DOMAIN_SETUP, "Initialising message dialog");
 
 	/* Get widgets */
-	ui =
-		twitux_xml_get_file (XML_FILE,
+	ui = twitux_xml_get_file (XML_FILE,
 							 "send_message_dialog", &priv->dialog,
 							 "send_message_textview", &priv->textview,
 							 "char_label", &priv->label,
Index: src/twitux-ui-utils.c
===================================================================
--- src/twitux-ui-utils.c	(revision 373)
+++ src/twitux-ui-utils.c	(working copy)
@@ -93,16 +93,16 @@
 twitux_help_show (GtkWindow *parent)
 {
 	GdkScreen *screen;
-	GError    *err = NULL;
+	GError    *error = NULL;
 
 	screen = gtk_widget_get_screen (GTK_WIDGET (parent));
 
 	gtk_show_uri (screen,
 				  "ghelp:twitux",
 				  gtk_get_current_event_time (),
-				  &err);
+				  &error);
 
-	if (err != NULL) {
+	if (error != NULL) {
 		GtkWidget *w;
 
 		w =	gtk_message_dialog_new (GTK_WINDOW (parent),
@@ -112,12 +112,12 @@
 									_("Unable to open help file"));
 
 		gtk_message_dialog_format_secondary_text (GTK_MESSAGE_DIALOG (w),
-												  "%s", err->message);
+												  "%s", error->message);
 
 		g_signal_connect (w, "response",
 						  G_CALLBACK (gtk_widget_destroy),
 						  NULL);
 		gtk_window_present (GTK_WINDOW (w));
-		g_error_free (err);
+		if(error) g_error_free (error);
 	}
 }
Index: src/twitux-app.c
===================================================================
--- src/twitux-app.c	(revision 373)
+++ src/twitux-app.c	(working copy)
@@ -20,6 +20,7 @@
  *
  * Authors: Brian Pepple <bpepple@fedoraproject.org>
  *			Daniel Morales <daniminas@gmail.com>
+ *			Kaity G. B. <uberChicGeekChick@openSUSE.us>
  */
 
 #include <config.h>
@@ -1466,24 +1467,21 @@
 twitux_app_set_image (const gchar *file,
                       GtkTreeIter  iter)
 {
-	GtkListStore *store;
 	GdkPixbuf	 *pixbuf, *resized;
 	GError		 *error = NULL;
 
-	pixbuf = gdk_pixbuf_new_from_file (file, &error);
-
-	if (!pixbuf){
-		twitux_debug (DEBUG_DOMAIN_SETUP, "Image error: %s: %s",
-					  file, error->message);
-		g_error_free (error);
+	if( !(pixbuf=gdk_pixbuf_new_from_file(file, &error)) ){
+		twitux_debug(DEBUG_DOMAIN_SETUP, "Image error: %s: %s", file, error->message);
+		if(error) g_error_free(error);
 		return;
 	}
 
-	resized = gdk_pixbuf_scale_simple(pixbuf, 48, 48, GDK_INTERP_BILINEAR);
+	resized=gdk_pixbuf_scale_simple( pixbuf, 48, 48, GDK_INTERP_BILINEAR );
 
-	store = twitux_tweet_list_get_store ();
-	gtk_list_store_set (store, &iter, PIXBUF_AVATAR, resized, -1);
+	gtk_list_store_set( (twitux_tweet_list_get_store()) , &iter, PIXBUF_AVATAR, resized, -1);
 	g_object_unref(pixbuf);
+	g_object_unref(resized);
+	if(error) g_error_free(error);
 }
 
 void
@@ -1505,10 +1503,11 @@
 	g_free (title_text);
 	
 	if (pixbuf) {
-		resized = gdk_pixbuf_scale_simple(pixbuf, 73, 73, GDK_INTERP_BILINEAR);
-		gtk_image_set_from_pixbuf (GTK_IMAGE (priv->expand_image), resized);
+		resized=gdk_pixbuf_scale_simple( pixbuf, 73, 73, GDK_INTERP_BILINEAR );
+		gtk_image_set_from_pixbuf(GTK_IMAGE (priv->expand_image), resized);
 		g_object_unref(resized);
 	}
 	
 	gtk_widget_show (priv->expand_box);
+	g_object_unref(priv);
 }
Index: src/twitux-lists-dialog.c
===================================================================
--- src/twitux-lists-dialog.c	(revision 373)
+++ src/twitux-lists-dialog.c	(working copy)
@@ -170,7 +170,7 @@
 						"following_list", "row-activated", list_follower_activated_cb,
 						NULL);
 
-	g_object_unref (ui);
+	gtk_widget_destroy (ui);
 
 	/* Set the parent */
 	g_object_add_weak_pointer (G_OBJECT (lists->dialog), (gpointer) &lists);
Index: src/twitux-tweet-list.c
===================================================================
--- src/twitux-tweet-list.c	(revision 373)
+++ src/twitux-tweet-list.c	(working copy)
@@ -172,7 +172,7 @@
 												  renderer,
 												  "markup", STRING_TEXT,
 												  NULL);
-	gtk_tree_view_column_set_sizing (tweet_column, GTK_TREE_VIEW_COLUMN_FIXED);
+	gtk_tree_view_column_set_sizing ( tweet_column, GTK_TREE_VIEW_COLUMN_FIXED);
 	g_object_set (renderer,
 				  "ypad", 0,
 				  "xpad", 5,
@@ -212,7 +212,7 @@
 	
 	if (!gtk_tree_selection_get_selected (sel, NULL, &iter))
 		return;
-
+	
 	gtk_tree_model_get (GTK_TREE_MODEL (priv->store),
 						&iter,
 						STRING_AUTHOR, &name,
@@ -220,7 +220,7 @@
 						STRING_DATE, &date,
 						PIXBUF_AVATAR, &pixbuf,
 						-1);
-
+	
 	twitux_app_expand_message (name, date, tweet, pixbuf);
 	
 	g_free (name);
Index: src/twitux-spell.c
===================================================================
--- src/twitux-spell.c	(revision 373)
+++ src/twitux-spell.c	(working copy)
@@ -128,7 +128,7 @@
 static void
 spell_iso_code_names_init (void)
 {
-	GError *err = NULL;
+	GError *error = NULL;
 	gchar  *buf;
 	gsize   buf_len;
 
@@ -139,7 +139,7 @@
 	bind_textdomain_codeset ("iso_639", "UTF-8");
 
 	/* FIXME: We should read this in chunks and pass to the parser. */
-	if (g_file_get_contents (ISO_CODES_DATADIR "/iso_639.xml", &buf, &buf_len, &err)) {
+	if (g_file_get_contents (ISO_CODES_DATADIR "/iso_639.xml", &buf, &buf_len, &error)) {
 		GMarkupParseContext *ctx;
 		GMarkupParser        parser = {
 			spell_iso_codes_parse_start_tag,
@@ -147,19 +147,19 @@
 		};
 
 		ctx = g_markup_parse_context_new (&parser, 0, NULL, NULL);
-		if (!g_markup_parse_context_parse (ctx, buf, buf_len, &err)) {
+		if (!g_markup_parse_context_parse (ctx, buf, buf_len, &error)) {
 			g_warning ("Failed to parse '%s': %s",
 					   ISO_CODES_DATADIR"/iso_639.xml",
-					   err->message);
-			g_error_free (err);
+					   error->message);
+			if(error) g_error_free (error);
 		}
 
 		g_markup_parse_context_free (ctx);
 		g_free (buf);
 	} else {
 		g_warning ("Failed to load '%s': %s",
-				ISO_CODES_DATADIR"/iso_639.xml", err->message);
-		g_error_free (err);
+				ISO_CODES_DATADIR"/iso_639.xml", error->message);
+		if(error) g_error_free (error);
 	}
 }
 
Index: src/Makefile.am
===================================================================
--- src/Makefile.am	(revision 373)
+++ src/Makefile.am	(working copy)
@@ -33,7 +33,7 @@
 	twitux-spell-dialog.c		twitux-spell-dialog.h		\
 	twitux-ui-utils.c		twitux-ui-utils.h		\
 	twitux-lists-dialog.c		twitux-lists-dialog.h		\
-	twitux-tweet-list.c		twitux-tweet-list.h
+	twitux-tweet-list.c		twitux-tweet-list.h		
 
 twitux_LDADD=						\
 	$(top_builddir)/libtwitux/libtwitux.la		\
Index: ChangeLog
===================================================================
--- ChangeLog	(revision 373)
+++ ChangeLog	(working copy)
@@ -1,15 +1,59 @@
-2009-02-13  Brian Pepple  <bpepple@fedoraproject.org>
+2009-02-11  Kaity G. B. <uberChicGeekChick@openSUSE.us>
 
-	* Makefile.am:
-	Remove intltool-* from Makefile.am to make it work with newer intltool.
+	* po/Makefile.in: PO_LINGUAS & USER_LINGUAS 
+		I added export GREP_OPTIONS=""; to both shell execs so it
+		actually finds files in the format it expects.
+	
+	* data/Makefile.in: twitux.desktop
+		changed:
+		twitux.desktop.in: $(top_builddir)/config.status $(srcdir)/twitux.desktop.in.in
+		to
+		twitux.desktop: $(top_builddir)/config.status $(srcdir)/twitux.desktop.in
+	
+	* help/twitux-omf.in: depricated.
 
-2009-02-11  Brian Pepple  <bpepple@fedoraproject.org>
+2009-02-11  Kaity G. B. <uberChicGeekChick@openSUSE.us>
 
-	* src/twitux-app.c:
-	* src/twitux-tweet-list.c:
-	Plug a leak with some pixbufs, and set the size of
-	avatar pixbuf.  (Patch provided by Kaity G. B.)
+	* configure: ac_config_files
+		I modified them so they look for 'po/Makefile' and
+		'data/twitux.desktop' correctly an will fall back to the
+		correct '.in' when needed.
 
+	* po/Makefile.in: clean & dist distdirs
+		I corrected the sh commands that find gettext .po, .gmo, & etc
+		files.  Now po/Makefile's clean & beyond will now work.  Most
+		importantly make clean will now work.
+	
+	* po/POFILES: twitux.desktop.in.in
+	* po/POFILES.in: twitux.desktop.in.in
+		These were both referencing twitux.desktop.in.in which was
+		only a duplicate of twitux.desktop.in.  They now correctly
+		look for twitux.desktop.in
+
+	* data/Makefile.am: .PRECIOUS: Makefile and multiple varibles
+	* data/Makefile.in: .PRECIOUS: Makefile and multiple varibles
+		I changed references to Makefile.in.in &
+		twitux-desktop.in.in to point to Makefile.in &
+		twitux.desktop.in respectfully.  I also removed the unnead
+		'.in.in' files.
+
+2009-02-09  Kaity G. B. <uberChicGeekChick@openSUSE.us>
+
+	* src/twitux-app.c: twitux_app_set_image & twitux_app_expand_message
+	* src/twitux-tweet-list.c: tweet_list_changed_cb
+		I plugged some large memory leaks by unreferencing
+		GdkPixbuf(s) & various other gobjects used my Twitux for
+		Twitter's avatars.
+
+2009-02-06  Kaity G. B. <uberChicGeekChick@openSUSE.us>
+
+	* src/twitux-app.c: twitux_app_set_image & twitux_app_expand_message
+	* src/twitux-tweet-list.c: tweet_list_setup_view
+		I wrote resizing code to stop large images from bloating
+		Twitux's tweet list.  I have also added code to keep the
+		tweet list avatar column at a fixed width.
+
+
 2009-01-31  Daniel Morales  <daniminas@gmail.com>
 
 	* src/twitux-network.c: Bugfix to Add/Remove friends methods,
Index: data/Makefile.am
===================================================================
--- data/Makefile.am	(revision 373)
+++ data/Makefile.am	(working copy)
@@ -1,6 +1,6 @@
 desktopdir 	 = $(datadir)/applications
-desktop_in_files = twitux.desktop.in.in
-desktop_DATA 	 = $(desktop_in_files:.desktop.in.in=.desktop)
+desktop_in_files = twitux.desktop.in
+desktop_DATA 	 = $(desktop_in_files:.desktop.in=.desktop)
 @INTLTOOL_DESKTOP_RULE@
 
 schemasdir	 = $(GCONF_SCHEMA_FILE_DIR)
Index: Makefile.am
===================================================================
--- Makefile.am	(revision 373)
+++ Makefile.am	(working copy)
@@ -4,6 +4,11 @@
 SUBDIRS += help
 endif
 
+INTLTOOL = 			\
+	intltool-extract.in 	\
+	intltool-merge.in 	\
+	intltool-update.in
+
 EXTRA_DIST = 			\
 	ChangeLog               \
 	README                  \
@@ -11,9 +16,13 @@
 	NEWS			\
 	xmldocs.make		\
 	omf.make		\
-	gnome-doc-utils.make
+	gnome-doc-utils.make	\
+	$(INTLTOOL)
 
 DISTCLEANFILES = 		\
+	intltool-extract	\
+	intltool-merge		\
+	intltool-update		\
 	gnome-doc-utils.make
 
 DISTCHECK_CONFIGURE_FLAGS =	\
Index: po/pt.po
===================================================================
--- po/pt.po	(revision 373)
+++ po/pt.po	(working copy)
@@ -138,7 +138,7 @@
 msgid "Twitter Account"
 msgstr "Conta Twitter"
 
-#: ../data/main.glade.h:30 ../data/twitux.desktop.in.in.h:2
+#: ../data/main.glade.h:30 ../data/twitux.desktop.in.h:2
 #: ../src/twitux-main.c:46 ../src/twitux-preferences.c:416
 msgid "Twitux"
 msgstr "Twitux"
@@ -227,7 +227,7 @@
 msgid "gtk-quit"
 msgstr "gtk-quit"
 
-#: ../data/twitux.desktop.in.in.h:1
+#: ../data/twitux.desktop.in.h:1
 msgid "A Twitter client"
 msgstr "Um cliente Twitter"
 
Index: po/es.po
===================================================================
--- po/es.po	(revision 373)
+++ po/es.po	(working copy)
@@ -101,7 +101,7 @@
 msgid "Send Direct Message"
 msgstr "Enviar Mensajes Directos"
 
-#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.in.h:2
+#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.h:2
 #: ../src/twitux-main.c:46 ../src/twitux-preferences.c:430
 msgid "Twitux"
 msgstr "Twitux"
@@ -243,7 +243,7 @@
 msgid "_Show Twitux"
 msgstr "_Mostrar Twitux"
 
-#: ../data/twitux.desktop.in.in.h:1
+#: ../data/twitux.desktop.in.h:1
 msgid "A Twitter client"
 msgstr "Un cliente Twitter"
 
Index: po/ja.po
===================================================================
--- po/ja.po	(revision 373)
+++ po/ja.po	(working copy)
@@ -99,7 +99,7 @@
 msgid "Send Direct Message"
 msgstr "ダイレクトメッセージ送信"
 
-#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.in.h:2
+#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.h:2
 #: ../src/twitux-main.c:46 ../src/twitux-preferences.c:430
 msgid "Twitux"
 msgstr ""
@@ -241,7 +241,7 @@
 msgid "_Show Twitux"
 msgstr "Twituxを表示(_S)"
 
-#: ../data/twitux.desktop.in.in.h:1
+#: ../data/twitux.desktop.in.h:1
 msgid "A Twitter client"
 msgstr "Twitterクライアント"
 
Index: po/POTFILES.in
===================================================================
--- po/POTFILES.in	(revision 373)
+++ po/POTFILES.in	(working copy)
@@ -8,7 +8,7 @@
 [type: gettext/glade]data/prefs_dlg.xml
 [type: gettext/glade]data/send_message_dlg.xml
 [type: gettext/glade]data/spell_dlg.xml
-data/twitux.desktop.in.in
+data/twitux.desktop.in
 data/twitux.schemas.in
 
 src/twitux-about.c
Index: AUTHORS
===================================================================
--- AUTHORS	(revision 373)
+++ AUTHORS	(working copy)
@@ -7,7 +7,10 @@
 
  * Brian Pepple  <bpepple@fedoraproject.org>
 
+--- Various fixes, patches, & improvements
 
+ * Kaity G. B. <uberChicGeekChick@openSUSE.us>
+
 --- Translators:
 
  * Swedish - Daniel Nylander <po@danielnylander.se>
Index: src/twitux-send-message-dialog.c
===================================================================
--- src/twitux-send-message-dialog.c	(revision 373)
+++ src/twitux-send-message-dialog.c	(working copy)
@@ -138,8 +138,7 @@
 	twitux_debug (DEBUG_DOMAIN_SETUP, "Initialising message dialog");
 
 	/* Get widgets */
-	ui =
-		twitux_xml_get_file (XML_FILE,
+	ui = twitux_xml_get_file (XML_FILE,
 							 "send_message_dialog", &priv->dialog,
 							 "send_message_textview", &priv->textview,
 							 "char_label", &priv->label,
Index: src/twitux-ui-utils.c
===================================================================
--- src/twitux-ui-utils.c	(revision 373)
+++ src/twitux-ui-utils.c	(working copy)
@@ -93,16 +93,16 @@
 twitux_help_show (GtkWindow *parent)
 {
 	GdkScreen *screen;
-	GError    *err = NULL;
+	GError    *error = NULL;
 
 	screen = gtk_widget_get_screen (GTK_WIDGET (parent));
 
 	gtk_show_uri (screen,
 				  "ghelp:twitux",
 				  gtk_get_current_event_time (),
-				  &err);
+				  &error);
 
-	if (err != NULL) {
+	if (error != NULL) {
 		GtkWidget *w;
 
 		w =	gtk_message_dialog_new (GTK_WINDOW (parent),
@@ -112,12 +112,12 @@
 									_("Unable to open help file"));
 
 		gtk_message_dialog_format_secondary_text (GTK_MESSAGE_DIALOG (w),
-												  "%s", err->message);
+												  "%s", error->message);
 
 		g_signal_connect (w, "response",
 						  G_CALLBACK (gtk_widget_destroy),
 						  NULL);
 		gtk_window_present (GTK_WINDOW (w));
-		g_error_free (err);
+		if(error) g_error_free (error);
 	}
 }
Index: src/twitux-app.c
===================================================================
--- src/twitux-app.c	(revision 373)
+++ src/twitux-app.c	(working copy)
@@ -20,6 +20,7 @@
  *
  * Authors: Brian Pepple <bpepple@fedoraproject.org>
  *			Daniel Morales <daniminas@gmail.com>
+ *			Kaity G. B. <uberChicGeekChick@openSUSE.us>
  */
 
 #include <config.h>
@@ -1466,24 +1467,21 @@
 twitux_app_set_image (const gchar *file,
                       GtkTreeIter  iter)
 {
-	GtkListStore *store;
 	GdkPixbuf	 *pixbuf, *resized;
 	GError		 *error = NULL;
 
-	pixbuf = gdk_pixbuf_new_from_file (file, &error);
-
-	if (!pixbuf){
-		twitux_debug (DEBUG_DOMAIN_SETUP, "Image error: %s: %s",
-					  file, error->message);
-		g_error_free (error);
+	if( !(pixbuf=gdk_pixbuf_new_from_file(file, &error)) ){
+		twitux_debug(DEBUG_DOMAIN_SETUP, "Image error: %s: %s", file, error->message);
+		if(error) g_error_free(error);
 		return;
 	}
 
-	resized = gdk_pixbuf_scale_simple(pixbuf, 48, 48, GDK_INTERP_BILINEAR);
+	resized=gdk_pixbuf_scale_simple( pixbuf, 48, 48, GDK_INTERP_BILINEAR );
 
-	store = twitux_tweet_list_get_store ();
-	gtk_list_store_set (store, &iter, PIXBUF_AVATAR, resized, -1);
+	gtk_list_store_set( (twitux_tweet_list_get_store()) , &iter, PIXBUF_AVATAR, resized, -1);
 	g_object_unref(pixbuf);
+	g_object_unref(resized);
+	if(error) g_error_free(error);
 }
 
 void
@@ -1505,10 +1503,12 @@
 	g_free (title_text);
 	
 	if (pixbuf) {
-		resized = gdk_pixbuf_scale_simple(pixbuf, 73, 73, GDK_INTERP_BILINEAR);
-		gtk_image_set_from_pixbuf (GTK_IMAGE (priv->expand_image), resized);
+		resized=gdk_pixbuf_scale_simple( pixbuf, 73, 73, GDK_INTERP_BILINEAR );
+		gtk_image_set_from_pixbuf(GTK_IMAGE (priv->expand_image), resized);
+		//g_object_unref(pixbuf);
 		g_object_unref(resized);
 	}
 	
 	gtk_widget_show (priv->expand_box);
+	g_object_unref(priv);
 }
Index: src/twitux-lists-dialog.c
===================================================================
--- src/twitux-lists-dialog.c	(revision 373)
+++ src/twitux-lists-dialog.c	(working copy)
@@ -170,7 +170,7 @@
 						"following_list", "row-activated", list_follower_activated_cb,
 						NULL);
 
-	g_object_unref (ui);
+	gtk_widget_destroy (ui);
 
 	/* Set the parent */
 	g_object_add_weak_pointer (G_OBJECT (lists->dialog), (gpointer) &lists);
Index: src/twitux-tweet-list.c
===================================================================
--- src/twitux-tweet-list.c	(revision 373)
+++ src/twitux-tweet-list.c	(working copy)
@@ -105,9 +105,11 @@
 
 	priv = GET_PRIV (object);
 
-	g_object_unref (priv->store);
+	if(priv->store)
+		g_object_unref (priv->store);
 
 	G_OBJECT_CLASS (twitux_tweet_list_parent_class)->finalize (object);
+	g_object_unref( object );
 }
 
 static void
@@ -172,7 +174,7 @@
 												  renderer,
 												  "markup", STRING_TEXT,
 												  NULL);
-	gtk_tree_view_column_set_sizing (tweet_column, GTK_TREE_VIEW_COLUMN_FIXED);
+	gtk_tree_view_column_set_sizing ( tweet_column, GTK_TREE_VIEW_COLUMN_FIXED);
 	g_object_set (renderer,
 				  "ypad", 0,
 				  "xpad", 5,
@@ -212,7 +214,7 @@
 	
 	if (!gtk_tree_selection_get_selected (sel, NULL, &iter))
 		return;
-
+	
 	gtk_tree_model_get (GTK_TREE_MODEL (priv->store),
 						&iter,
 						STRING_AUTHOR, &name,
@@ -220,7 +222,7 @@
 						STRING_DATE, &date,
 						PIXBUF_AVATAR, &pixbuf,
 						-1);
-
+	
 	twitux_app_expand_message (name, date, tweet, pixbuf);
 	
 	g_free (name);
Index: src/twitux-spell.c
===================================================================
--- src/twitux-spell.c	(revision 373)
+++ src/twitux-spell.c	(working copy)
@@ -128,7 +128,7 @@
 static void
 spell_iso_code_names_init (void)
 {
-	GError *err = NULL;
+	GError *error = NULL;
 	gchar  *buf;
 	gsize   buf_len;
 
@@ -139,7 +139,7 @@
 	bind_textdomain_codeset ("iso_639", "UTF-8");
 
 	/* FIXME: We should read this in chunks and pass to the parser. */
-	if (g_file_get_contents (ISO_CODES_DATADIR "/iso_639.xml", &buf, &buf_len, &err)) {
+	if (g_file_get_contents (ISO_CODES_DATADIR "/iso_639.xml", &buf, &buf_len, &error)) {
 		GMarkupParseContext *ctx;
 		GMarkupParser        parser = {
 			spell_iso_codes_parse_start_tag,
@@ -147,19 +147,19 @@
 		};
 
 		ctx = g_markup_parse_context_new (&parser, 0, NULL, NULL);
-		if (!g_markup_parse_context_parse (ctx, buf, buf_len, &err)) {
+		if (!g_markup_parse_context_parse (ctx, buf, buf_len, &error)) {
 			g_warning ("Failed to parse '%s': %s",
 					   ISO_CODES_DATADIR"/iso_639.xml",
-					   err->message);
-			g_error_free (err);
+					   error->message);
+			if(error) g_error_free (error);
 		}
 
 		g_markup_parse_context_free (ctx);
 		g_free (buf);
 	} else {
 		g_warning ("Failed to load '%s': %s",
-				ISO_CODES_DATADIR"/iso_639.xml", err->message);
-		g_error_free (err);
+				ISO_CODES_DATADIR"/iso_639.xml", error->message);
+		if(error) g_error_free (error);
 	}
 }
 
Index: src/Makefile.am
===================================================================
--- src/Makefile.am	(revision 373)
+++ src/Makefile.am	(working copy)
@@ -33,7 +33,7 @@
 	twitux-spell-dialog.c		twitux-spell-dialog.h		\
 	twitux-ui-utils.c		twitux-ui-utils.h		\
 	twitux-lists-dialog.c		twitux-lists-dialog.h		\
-	twitux-tweet-list.c		twitux-tweet-list.h
+	twitux-tweet-list.c		twitux-tweet-list.h		
 
 twitux_LDADD=						\
 	$(top_builddir)/libtwitux/libtwitux.la		\
Index: ChangeLog
===================================================================
--- ChangeLog	(revision 373)
+++ ChangeLog	(working copy)
@@ -1,15 +1,59 @@
-2009-02-13  Brian Pepple  <bpepple@fedoraproject.org>
+2009-02-11  Kaity G. B. <uberChicGeekChick@openSUSE.us>
 
-	* Makefile.am:
-	Remove intltool-* from Makefile.am to make it work with newer intltool.
+	* po/Makefile.in: PO_LINGUAS & USER_LINGUAS 
+		I added export GREP_OPTIONS=""; to both shell execs so it
+		actually finds files in the format it expects.
+	
+	* data/Makefile.in: twitux.desktop
+		changed:
+		twitux.desktop.in: $(top_builddir)/config.status $(srcdir)/twitux.desktop.in.in
+		to
+		twitux.desktop: $(top_builddir)/config.status $(srcdir)/twitux.desktop.in
+	
+	* help/twitux-omf.in: depricated.
 
-2009-02-11  Brian Pepple  <bpepple@fedoraproject.org>
+2009-02-11  Kaity G. B. <uberChicGeekChick@openSUSE.us>
 
-	* src/twitux-app.c:
-	* src/twitux-tweet-list.c:
-	Plug a leak with some pixbufs, and set the size of
-	avatar pixbuf.  (Patch provided by Kaity G. B.)
+	* configure: ac_config_files
+		I modified them so they look for 'po/Makefile' and
+		'data/twitux.desktop' correctly an will fall back to the
+		correct '.in' when needed.
 
+	* po/Makefile.in: clean & dist distdirs
+		I corrected the sh commands that find gettext .po, .gmo, & etc
+		files.  Now po/Makefile's clean & beyond will now work.  Most
+		importantly make clean will now work.
+	
+	* po/POFILES: twitux.desktop.in.in
+	* po/POFILES.in: twitux.desktop.in.in
+		These were both referencing twitux.desktop.in.in which was
+		only a duplicate of twitux.desktop.in.  They now correctly
+		look for twitux.desktop.in
+
+	* data/Makefile.am: .PRECIOUS: Makefile and multiple varibles
+	* data/Makefile.in: .PRECIOUS: Makefile and multiple varibles
+		I changed references to Makefile.in.in &
+		twitux-desktop.in.in to point to Makefile.in &
+		twitux.desktop.in respectfully.  I also removed the unnead
+		'.in.in' files.
+
+2009-02-09  Kaity G. B. <uberChicGeekChick@openSUSE.us>
+
+	* src/twitux-app.c: twitux_app_set_image & twitux_app_expand_message
+	* src/twitux-tweet-list.c: tweet_list_changed_cb
+		I plugged some large memory leaks by unreferencing
+		GdkPixbuf(s) & various other gobjects used my Twitux for
+		Twitter's avatars.
+
+2009-02-06  Kaity G. B. <uberChicGeekChick@openSUSE.us>
+
+	* src/twitux-app.c: twitux_app_set_image & twitux_app_expand_message
+	* src/twitux-tweet-list.c: tweet_list_setup_view
+		I wrote resizing code to stop large images from bloating
+		Twitux's tweet list.  I have also added code to keep the
+		tweet list avatar column at a fixed width.
+
+
 2009-01-31  Daniel Morales  <daniminas@gmail.com>
 
 	* src/twitux-network.c: Bugfix to Add/Remove friends methods,
Index: data/Makefile.am
===================================================================
--- data/Makefile.am	(revision 373)
+++ data/Makefile.am	(working copy)
@@ -1,6 +1,6 @@
 desktopdir 	 = $(datadir)/applications
-desktop_in_files = twitux.desktop.in.in
-desktop_DATA 	 = $(desktop_in_files:.desktop.in.in=.desktop)
+desktop_in_files = twitux.desktop.in
+desktop_DATA 	 = $(desktop_in_files:.desktop.in=.desktop)
 @INTLTOOL_DESKTOP_RULE@
 
 schemasdir	 = $(GCONF_SCHEMA_FILE_DIR)
Index: Makefile.am
===================================================================
--- Makefile.am	(revision 373)
+++ Makefile.am	(working copy)
@@ -4,6 +4,11 @@
 SUBDIRS += help
 endif
 
+INTLTOOL = 			\
+	intltool-extract.in 	\
+	intltool-merge.in 	\
+	intltool-update.in
+
 EXTRA_DIST = 			\
 	ChangeLog               \
 	README                  \
@@ -11,9 +16,13 @@
 	NEWS			\
 	xmldocs.make		\
 	omf.make		\
-	gnome-doc-utils.make
+	gnome-doc-utils.make	\
+	$(INTLTOOL)
 
 DISTCLEANFILES = 		\
+	intltool-extract	\
+	intltool-merge		\
+	intltool-update		\
 	gnome-doc-utils.make
 
 DISTCHECK_CONFIGURE_FLAGS =	\
Index: po/pt.po
===================================================================
--- po/pt.po	(revision 373)
+++ po/pt.po	(working copy)
@@ -138,7 +138,7 @@
 msgid "Twitter Account"
 msgstr "Conta Twitter"
 
-#: ../data/main.glade.h:30 ../data/twitux.desktop.in.in.h:2
+#: ../data/main.glade.h:30 ../data/twitux.desktop.in.h:2
 #: ../src/twitux-main.c:46 ../src/twitux-preferences.c:416
 msgid "Twitux"
 msgstr "Twitux"
@@ -227,7 +227,7 @@
 msgid "gtk-quit"
 msgstr "gtk-quit"
 
-#: ../data/twitux.desktop.in.in.h:1
+#: ../data/twitux.desktop.in.h:1
 msgid "A Twitter client"
 msgstr "Um cliente Twitter"
 
Index: po/es.po
===================================================================
--- po/es.po	(revision 373)
+++ po/es.po	(working copy)
@@ -101,7 +101,7 @@
 msgid "Send Direct Message"
 msgstr "Enviar Mensajes Directos"
 
-#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.in.h:2
+#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.h:2
 #: ../src/twitux-main.c:46 ../src/twitux-preferences.c:430
 msgid "Twitux"
 msgstr "Twitux"
@@ -243,7 +243,7 @@
 msgid "_Show Twitux"
 msgstr "_Mostrar Twitux"
 
-#: ../data/twitux.desktop.in.in.h:1
+#: ../data/twitux.desktop.in.h:1
 msgid "A Twitter client"
 msgstr "Un cliente Twitter"
 
Index: po/ja.po
===================================================================
--- po/ja.po	(revision 373)
+++ po/ja.po	(working copy)
@@ -99,7 +99,7 @@
 msgid "Send Direct Message"
 msgstr "ダイレクトメッセージ送信"
 
-#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.in.h:2
+#: ../data/main_window.xml.h:10 ../data/twitux.desktop.in.h:2
 #: ../src/twitux-main.c:46 ../src/twitux-preferences.c:430
 msgid "Twitux"
 msgstr ""
@@ -241,7 +241,7 @@
 msgid "_Show Twitux"
 msgstr "Twituxを表示(_S)"
 
-#: ../data/twitux.desktop.in.in.h:1
+#: ../data/twitux.desktop.in.h:1
 msgid "A Twitter client"
 msgstr "Twitterクライアント"
 
Index: po/POTFILES.in
===================================================================
--- po/POTFILES.in	(revision 373)
+++ po/POTFILES.in	(working copy)
@@ -8,7 +8,7 @@
 [type: gettext/glade]data/prefs_dlg.xml
 [type: gettext/glade]data/send_message_dlg.xml
 [type: gettext/glade]data/spell_dlg.xml
-data/twitux.desktop.in.in
+data/twitux.desktop.in
 data/twitux.schemas.in
 
 src/twitux-about.c
